Flask>=3.0.0
Flask-SQLAlchemy>=3.1.1
Werkzeug>=3.0.1
opencv-python-headless>=4.8.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
pytest>=7.4.0
python-dotenv>=1.0.0
Pillow>=10.0.0
gunicorn>=21.2.0
# Note: face_recognition requires dlib (which compiles from C++ source and exceeds free build RAM on cloud hosts).
# The system automatically utilizes high-performance OpenCV biometric vectorization as its built-in engine.
